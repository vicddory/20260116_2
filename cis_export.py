import matplotlib.pyplot as plt
import matplotlib.font_manager as fm
import os

# 1. 폰트 파일 경로 설정 (파일명 대소문자 주의: NanumGothic.ttf)
font_path = os.path.join(os.getcwd(), "NanumGothic.ttf")

# 2. 폰트가 있는지 확인 후 적용
if os.path.exists(font_path):
    # 폰트 매니저에 폰트 추가
    prop = fm.FontProperties(fname=font_path)
    # Matplotlib 전역 설정 업데이트
    plt.rcParams['font.family'] = prop.get_name()
    plt.rcParams['axes.unicode_minus'] = False
    
    # 캐시된 폰트 리스트에 강제 등록
    fm.fontManager.addfont(font_path)
    
    # 💡 핵심: 현재 활성화된 테마의 폰트까지 강제로 고정
    plt.rc('font', family=prop.get_name())
else:
    # 폰트가 없을 때 화면에 경로를 출력해서 확인 (디버깅용)
    import streamlit as st
    st.error(f"폰트를 찾지 못했습니다. 경로 확인: {font_path}")

# --- 이후에 그래프 그리는 코드 작성 ---
# fig, ax = plt.subplots(...)
# sns.barplot(..., ax=ax)
import streamlit as st
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
import platform


# -----------------------------------------------------------------------------
# 2. 웹페이지 기본 설정
# -----------------------------------------------------------------------------
st.set_page_config(
    page_title="CIS 지역본부 유사도 분석",
    page_icon="🌍",
    layout="wide"
)

st.title("🌍 CIS 지역본부 국가 간 유사도 분석")
st.markdown("공공데이터포털의 **'CIS지역본부'** 데이터를 활용하여 국가별 코사인 유사도를 시각화합니다.")


# -----------------------------------------------------------------------------
# 3. 데이터 불러오기
# -----------------------------------------------------------------------------
# @st.cache_data는 데이터를 매번 새로 읽지 않고 메모리에 저장해두어 속도를 높입니다.
@st.cache_data
def load_data():
    file_path = "./data/CIS지역본부.csv" # 파일 경로 (같은 폴더에 있어야 함)
    
    # 공공데이터는 보통 'cp949' 인코딩을 사용합니다.
    # 만약 에러가 나면 자동으로 'utf-8'로 다시 시도하도록 작성했습니다.
    try:
        df = pd.read_csv(file_path, encoding='cp949')
    except UnicodeDecodeError:
        df = pd.read_csv(file_path, encoding='utf-8')
    return df

try:
    df = load_data()
    st.success("데이터를 성공적으로 불러왔습니다!")
    
    # 데이터 미리보기 (체크박스를 클릭하면 데이터프레임이 열립니다)
    if st.checkbox("원본 데이터 보기"):
        st.dataframe(df)

except FileNotFoundError:
    st.error("CSV 파일을 찾을 수 없습니다. 파일명과 경로를 확인해주세요.")
    st.stop() # 파일이 없으면 아래 코드를 실행하지 않고 멈춤


# -----------------------------------------------------------------------------
# 4. 분석 1: 전체 국가 유사도 히트맵 (Heatmap)
# -----------------------------------------------------------------------------
st.header("1. 국가 간 유사도 전체 지도 (Heatmap)")
st.info("색이 진할수록(또는 밝을수록) 두 국가 간의 유사도가 높음을 의미합니다.")

# 히트맵을 그리려면 데이터를 '행렬(Matrix)' 형태로 바꿔야 합니다.
# 행(index): 국가명1, 열(columns): 국가명2, 값(values): 코사인 유사도
df_pivot = df.pivot(index="국가명1", columns="국가명2", values="코사인 유사도")

# 그래프 그리기
fig1, ax1 = plt.subplots(figsize=(12, 10)) # 도화지 크기 설정

# seaborn의 heatmap 함수 사용
sns.heatmap(data=df_pivot, 
            annot=True,      # 칸 안에 숫자 표시 여부
            fmt=".2f",       # 소수점 둘째 자리까지 표시
            cmap="Blues",    # 색상 테마 (파란색 계열)
            ax=ax1)          # 위에서 만든 ax1 영역에 그림

ax1.set_title("CIS 국가 간 코사인 유사도 히트맵")
st.pyplot(fig1)


# -----------------------------------------------------------------------------
# 5. 분석 2: 특정 국가 중심 비교 (Bar Chart)
# -----------------------------------------------------------------------------
st.header("2. 특정 국가 중심 상세 분석")

# 분석할 기준 국가 선택하기 (중복 제거된 국가 목록 사용)
country_list = df['국가명1'].unique()
selected_country = st.selectbox("기준이 될 국가를 선택하세요:", country_list)

if selected_country:
    # 선택한 국가의 데이터만 필터링 (자신과의 비교인 1.0은 제외 가능)
    # 여기서는 자신(유사도 1.0)을 포함하여 비교합니다.
    filtered_df = df[df['국가명1'] == selected_country]
    
    # 유사도가 높은 순서대로 정렬 (내림차순)
    sorted_df = filtered_df.sort_values(by="코사인 유사도", ascending=False)
    
    # 막대 그래프 그리기
    fig2, ax2 = plt.subplots(figsize=(10, 6))
    
    # x축: 유사도 값, y축: 상대 국가명
    sns.barplot(x="코사인 유사도", y="국가명2", data=sorted_df, palette="viridis", ax=ax2)
    
    # 그래프 꾸미기
    ax2.set_title(f"[{selected_country}] 와(과) 다른 국가들의 유사도 순위")
    ax2.set_xlabel("코사인 유사도")
    ax2.set_ylabel("비교 국가")
    
    # 막대 끝에 수치 표시하기 (조금 더 고급 기능)
    for p in ax2.patches:
        ax2.annotate(f'{p.get_width():.2f}', 
                     (p.get_width(), p.get_y() + p.get_height() / 2), 
                     ha='left', va='center', xytext=(5, 0), textcoords='offset points')

    st.pyplot(fig2)